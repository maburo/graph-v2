// import * as React from 'react';

// import { FormattedHtmlMessage } from 'ib-i18n';

// export const formatHtmlMessage = (
//     message: string,
//     values?: Record<string, unknown>,
//     namespace?: string,
//     tagName?: string,
// ) => {
//     return <FormattedHtmlMessage message={message} namespace={namespace} tagName={tagName} {...values} />;
// };
